# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tooling
.pytest_cache/
.ty_cache/
.ruff_cache/
.ipynb_checkpoints/

# Pipeline outputs
*.parquet
*.npy
embeddings.csv

# OS
.DS_Store

# Secrets / local environment — never commit real tokens
.env
.env.*
!.env.example

# Claude Code — commit shared config (settings.json, hooks); ignore personal
# settings and session-local state (scheduled_tasks.lock, checkpoints, etc.).
.claude/*
!.claude/settings.json
!.claude/hooks/
